import os
import cv2
import numpy as np
import insightface
from sklearn import preprocessing

if __name__ == "__main__":
    l = list()
    l.append({1,2})
    l.append({
                    "user_name": user_name,
                    "feature": embedding
                })
    print(l)